Place your logo file here named: logo.png

Recommended size: 200x80px (landscape) or 80x80px (square/icon)
Supported formats: PNG (preferred, supports transparency), SVG, JPG

The login and register pages will automatically display it.
If the file is missing, a fallback recycle icon is shown instead.
